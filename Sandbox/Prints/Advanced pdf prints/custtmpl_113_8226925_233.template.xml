<?xml version="1.0"?>
<!DOCTYPE pdf PUBLIC "-//big.faceless.org//report" "report-1.1.dtd">
<pdf>
<head>
	<link name="NotoSans" type="font" subtype="truetype" src="${nsfont.NotoSans_Regular}" src-bold="${nsfont.NotoSans_Bold}" src-italic="${nsfont.NotoSans_Italic}" src-bolditalic="${nsfont.NotoSans_BoldItalic}" bytes="2" />
	<#if .locale == "zh_CN">
		<link name="NotoSansCJKsc" type="font" subtype="opentype" src="${nsfont.NotoSansCJKsc_Regular}" src-bold="${nsfont.NotoSansCJKsc_Bold}" bytes="2" />
	<#elseif .locale == "zh_TW">
		<link name="NotoSansCJKtc" type="font" subtype="opentype" src="${nsfont.NotoSansCJKtc_Regular}" src-bold="${nsfont.NotoSansCJKtc_Bold}" bytes="2" />
	<#elseif .locale == "ja_JP">
		<link name="NotoSansCJKjp" type="font" subtype="opentype" src="${nsfont.NotoSansCJKjp_Regular}" src-bold="${nsfont.NotoSansCJKjp_Bold}" bytes="2" />
	<#elseif .locale == "ko_KR">
		<link name="NotoSansCJKkr" type="font" subtype="opentype" src="${nsfont.NotoSansCJKkr_Regular}" src-bold="${nsfont.NotoSansCJKkr_Bold}" bytes="2" />
	<#elseif .locale == "th_TH">
		<link name="NotoSansThai" type="font" subtype="opentype" src="${nsfont.NotoSansThai_Regular}" src-bold="${nsfont.NotoSansThai_Bold}" bytes="2" />
	</#if>
    <macrolist>
        <macro id="nlheader">
            
<table class="header" style="width: 100%; padding-top: 0; margin: 0;" border="0"><tr>
<td><#if companyInformation.logoUrl?length != 0><@filecabinet nstype="image" style="float: left; width: 300px; height: 110px;" src="${companyInformation.logoUrl}" /></#if></td>
<td align="right"><span class="title">Vendor Credit</span><br />
<table style="table-layout: fixed; border: 0px; padding-top: 5px; height: 18.6632px;" width="70%"><!-- <tr>
                            <td colspan="4"><strong>Reference No:</strong></td>
                            <td colspan="4" align="right">${record.tranid}</td>
                          </tr> --><tr style="height: 18.6632px;">
<td style="height: 18.6632px;" colspan="4"><strong>Transaction Date:</strong></td>
<td style="height: 18.6632px;" colspan="4" align="right">${record.trandate}</td>
</tr>
<tr>
<td style="height: 18.6632px;" colspan="4"><strong>Reference No:</strong></td>
<td style="height: 18.6632px;" colspan="4" align="right">${record.transactionnumber}</td>
</tr></table>
</td>
</tr></table>

        </macro>
        <macro id="nlfooter">
            
<table class="footer"><tr>
<td align="right"><pagenumber/> of <totalpages/></td>
</tr></table>

        </macro>
    </macrolist>
    <style>
			* {
			<#if .locale == "zh_CN">
				font-family: NotoSans, NotoSansCJKsc, sans-serif;
			<#elseif .locale == "zh_TW">
				font-family: NotoSans, NotoSansCJKtc, sans-serif;
			<#elseif .locale == "ja_JP">
				font-family: NotoSans, NotoSansCJKjp, sans-serif;
			<#elseif .locale == "ko_KR">
				font-family: NotoSans, NotoSansCJKkr, sans-serif;
			<#elseif .locale == "th_TH">
				font-family: NotoSans, NotoSansThai, sans-serif;
			<#else>
				font-family: NotoSans, sans-serif;
			</#if>
			}
			table {
				font-size: 9pt;
				table-layout: fixed;
			}
			th {
				font-weight: bold;
				font-size: 8pt;
				vertical-align: middle;
				padding: 5px 6px 3px;
				background-color: #e3e3e3;
				color: #333333;
			}
			th.t
			td {
				padding: 4px 6px;
			}
			td p { align:left }
			b {
				font-weight: bold;
				color: #333333;
			}
			table.header td {
				padding: 0;
				font-size: 10pt;
			}
			table.footer td {
				padding: 0;
				font-size: 8pt;
			}
			table.itemtable th {
				padding-bottom: 10px;
				padding-top: 10px;
			}
			table.body td {
				padding-top: 2px;
			}
			table.total {
				page-break-inside: avoid;
			}
			span.title {
				font-size: 28pt;
			}
			span.tabletitle {
				font-size: 16pt;
			}
			span.transactionheader {
				font-size: 10pt;
			}
			span.number {
				font-size: 16pt;
			}
			hr {
				width: 100%;
				color: #d3d3d3;
				background-color: #d3d3d3;
				height: 1px;
			}
		</style>
</head>
<body header="nlheader" header-height="10%" footer="nlfooter" footer-height="10pt" padding="0.5in 0.5in 0.5in 0.5in" size="Letter">
    <!-- start transaction header -->
  <#assign exRate = record.exchangerate?replace("$","")?replace("PHP","")?replace(",","")?number>
<table class="itemtable" style="width: 100%; margin-top: 10px; height: 55.9896px;"><tr style="height: 18.6632px;">
<td style="height: 18.6632px;"><span class="transactionheader"><strong>${record.entity@label}:</strong>&nbsp;${record.entity}</span></td>
</tr>
<#if preferences.SUBSIDIARIES>
<tr style="height: 18.6632px;">
<td style="height: 18.6632px;"><span class="transactionheader"><strong>${record.subsidiary@label}:</strong>&nbsp;${record.subsidiary}</span></td>
</tr>
</#if>
<tr style="height: 18.6632px;">
<td style="height: 18.6632px;"><span class="transactionheader"><strong>${record.duedate@label}:</strong>&nbsp;${record.duedate}</span></td>
</tr></table>
<!-- end transaction header --> <!-- start items/expenses --> <#if (record.item?is_collection && record.item?has_content) || (record.expense?is_collection && record.expense?has_content)> <#if (record.item?is_collection && record.item?has_content) && (record.expense?is_collection && record.expense?has_content)> <!-- both items and expenses machine have content --> <span class="tabletitle">${record.item@label}/${record.expense@label}</span> <br />
<table class="itemtable" style="width: 100%; margin-top: 10px;">
<thead>
<tr>
<th style="width: 25.0162%;" colspan="2">${record.item[0].item@label}/${record.expense[0].account@label}</th>
<th style="width: 12.5085%;">${record.item[0].quantity@label}</th>
<th style="width: 12.5085%;" align="right">${record.item[0].taxrate1@label}</th>
<#if record.item[0].tax1amt@label?length != 0>
<th style="width: 12.5085%;" align="right">${record.item[0].tax1amt@label}</th>
</#if> <#if record.item[0].taxrate2@label?length != 0>
<th style="width: 12.5085%;" align="right">${record.item[0].taxrate2@label}</th>
</#if>
<th style="width: 12.5085%;" align="right">${record.item[0].rate@label}</th>
<th style="width: 12.5085%;" align="right">${record.item[0].amount@label}</th>
</tr>
</thead>
<!-- start items --> <#list record.item as item><tr>
<td style="width: 25.0162%;" colspan="2">${item.item}</td>
<td style="width: 12.5085%;">${item.quantity}</td>
<td style="width: 12.5085%;" align="right">${(item.taxrate1 * exRate)?string("#,##0.00")}</td>
<#if item.tax1amt?has_content>
<td style="width: 12.5085%;" align="right">${(item.tax1amt * exRate)?string("#,##0.00")}</td>
</#if> <#if item.taxrate2?has_content>
<td style="width: 12.5085%;" align="right">${(item.taxrate2 * exRate)?string("#,##0.00")}</td>
</#if>
<td style="width: 12.5085%;" align="right">${(item.rate * exRate)?string("#,##0.00")}</td>
<td style="width: 12.5085%;" align="right">${(item.amount * exRate)?string("#,##0.00")}</td>
</tr>
</#list> <!-- start expenses --> <#list record.expense as expense>
<tr>
<td style="width: 25.0162%;" colspan="2">${expense.account}</td>
<td style="width: 12.5085%;">&nbsp;</td>
<!-- expenses don't have quantities -->
<td style="width: 12.5085%;" align="right">${(expense.taxrate1 * exRate)?string("#,##0.00")}</td>
<#if expense.tax1amt?has_content>
<td style="width: 12.5085%;" align="right">${(expense.tax1amt * exRate)?string("#,##0.00")}</td>
</#if> <#if expense.taxrate2?has_content>
<td style="width: 12.5085%;" align="right">${(expense.taxrate2 * exRate)?string("#,##0.00")}</td>
</#if>
<td style="width: 12.5085%;">&nbsp;</td>
<!-- expenses don't have rates -->
<td style="width: 12.5085%;" align="right">${(expense.amount * exRate)?string("#,##0.00")}</td>
</tr>
</#list></table>
<#elseif record.item?is_collection && record.item?has_content> <!-- only items machine have content --> <span class="tabletitle">${record.item@label}</span> <br />
<table class="itemtable" style="width: 100%; margin-top: 10px; border: black;">
<thead>
<tr>
<th colspan="2">${record.item[0].item@label}</th>
<th>${record.item[0].quantity@label}</th>
<th align="right">${record.item[0].taxrate1@label}</th>
<#if record.item[0].tax1amt@label?length != 0>
<th align="right">${record.item[0].tax1amt@label}</th>
</#if> <#if record.item[0].taxrate2@label?length != 0>
<th align="right">${record.item[0].taxrate2@label}</th>
</#if>
<th align="right">${record.item[0].rate@label}</th>
<th align="right">${record.item[0].amount@label}</th>
</tr>
</thead>
<!-- start items --> <#list record.item as item><tr>
<td colspan="2">${item.item}</td>
<td>${item.quantity}</td>
<td align="right">${(item.taxrate1 * exRate)?string("#,##0.00")}</td>
<#if item.tax1amt?has_content>
<td align="right">${(item.tax1amt * exRate)?string("#,##0.00")}</td>
</#if> <#if item.taxrate2?has_content>
<td align="right">${(item.taxrate2 * exRate)?string("#,##0.00")}</td>
</#if>
<td align="right">${(item.rate * exRate)?string("#,##0.00")}</td>
<td align="right">${(item.amount * exRate)?string("#,##0.00")}</td>
</tr>
</#list></table>
<#else> <!-- only expenses machine have content --> <span class="tabletitle">${record.expense@label}</span> <br />
<table class="itemtable" style="width: 100%; margin-top: 10px;">
<thead>
<tr>
<th colspan="2">${record.expense[0].account@label}</th>
<th align="right">${record.expense[0].taxrate1@label}</th>
<#if record.expense[0].tax1amt@label?length != 0>
<th align="right">${record.expense[0].tax1amt@label}</th>
</#if> <#if record.expense[0].taxrate2@label?length != 0>
<th align="right">${record.expense[0].taxrate2@label}</th>
</#if>
<th align="right">${record.expense[0].amount@label}</th>
</tr>
</thead>
<!-- start expenses --> <#list record.expense as expense><tr>
<td colspan="2">${expense.account}</td>
<td align="right">${(expense.taxrate1 * exRate)?string("#,##0.00")}</td>
<#if expense.tax1amt?has_content>
<td align="right">${(expense.tax1amt * exRate)?string("#,##0.00")}</td>
</#if> <#if expense.taxrate2?has_content>
<td align="right">${(expense.taxrate2 * exRate)?string("#,##0.00")}</td>
</#if>
<td align="right">${(expense.amount * exRate)?string("#,##0.00")}</td>
</tr>
</#list></table>
</#if><hr /><!-- start items/expenses total -->
<table class="total" style="width: 100%; margin-top: 10px;"><#if record.taxtotal?has_content><tr>
<td colspan="4">&nbsp;</td>
<th align="right">${record.taxtotal@label}</th>
<td align="right">${(record.taxtotal * exRate)?string("#,##0.00")}</td>
</tr>
</#if> <#if record.tax2total?has_content>
<tr>
<td colspan="4">&nbsp;</td>
<th align="right">${record.tax2total@label}</th>
<td align="right">${(record.tax2total * exRate)?string("#,##0.00")}</td>
</tr>
</#if>
<tr>
<td colspan="4">&nbsp;</td>
<th align="right">${record.usertotal@label}</th>
<td align="right">${(record.usertotal * exRate)?string("#,##0.00")}</td>
</tr></table>
<!-- end items/expenses total --> </#if> <!-- end items/expenses --> <!-- start applications --> <#if record.apply?is_collection && record.apply?has_content> <span class="tabletitle">${record.apply@label}</span> <br /><!-- start applications machine -->
<table class="itemtable" style="width: 100%; margin-top: 10px;"><#list record.apply as apply> <#if apply_index == 0>
<thead>
<tr>
<th>${apply.type@label}</th>
<th>${apply.refnum@label}</th>
<th align="right">${apply.total@label}</th>
<th align="right">${apply.due@label}</th>
<th align="right">${apply.amount@label}</th>
</tr>
</thead>
</#if><tr>
<td>${apply.type}</td>
<td>${apply.refnum}</td>
<td align="right">${(apply.total * exRate)?string("#,##0.00")}</td>
<td align="right">${(apply.due * exRate)?string("#,##0.00")}</td>
<td align="right">${(apply.amount * exRate)?string("#,##0.00")}</td>
</tr>
</#list></table>
<!-- end applications machine --><hr /><!-- start applications total -->
<table class="total" style="width: 100%; margin-top: 10px;"><tr>
<td colspan="4">&nbsp;</td>
<th align="right">${record.applied@label}</th>
<td align="right">${(record.applied * exRate)?string("#,##0.00")}</td>
</tr>
<tr>
<td colspan="4">&nbsp;</td>
<th align="right">${record.unapplied@label}</th>
<td align="right">${(record.unapplied * exRate)?string("#,##0.00")}</td>
</tr></table>
<!-- end applications total --> </#if> <!-- end applications -->
</body>
</pdf>